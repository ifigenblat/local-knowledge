# LocalKnowledge legacy server (deprecated)

**Do not start this server.** It conflicts with ports used by the microservices (e.g. port 5001 = auth-service).

To run the app, use the microservices:

```bash
cd services
./start-all.sh
```

API entry point: gateway on port 8000.
