# Collection Service
PORT=5005

# MongoDB (same DB as other services). If MongoDB requires auth, use:
# MONGODB_URI=mongodb://USER:PASSWORD@localhost:27017/local-knowledge?authSource=admin
# Copy this file to .env and set MONGODB_URI to match your card-service/backend.
MONGODB_URI=mongodb://localhost:27017/local-knowledge
