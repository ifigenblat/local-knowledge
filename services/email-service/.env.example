# Email Service
PORT=5009

# SMTP (production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=user
# SMTP_PASS=password
# SMTP_FROM=noreply@localknowledge.com

# MailHog (local dev - no config needed, uses defaults)
# MAILHOG_HOST=127.0.0.1
# MAILHOG_PORT=1025
# View emails at http://127.0.0.1:8025

# Client URL for reset links
CLIENT_URL=http://localhost:3000
