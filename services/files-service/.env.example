# Files Service
PORT=5012

# PostgreSQL (same as other services). Set in services/.env or here.
# DATABASE_URL=postgresql://localknowledge:localknowledge@localhost:5432/localknowledge

# Path to uploads directory (where files are stored)
UPLOAD_DIR=../../server/uploads
