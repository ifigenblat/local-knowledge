# Preview Service
PORT=5011

# Path to uploads directory (where backend stores uploaded files)
# Default: resolved relative to cwd (../../server/uploads when run from services/)
UPLOAD_DIR=../../server/uploads
