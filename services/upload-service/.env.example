# Upload Service
PORT=5006

# Backend (monolith) URL for processing uploaded files
BACKEND_SERVICE_URL=http://localhost:5010

# Directory to store uploaded files (backend must be able to read this path)
# Default: ../../server/uploads relative to service root
# UPLOAD_DIR=/absolute/path/to/server/uploads
